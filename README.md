# SASS Boilerplate

## Requirements

- Node.js (22.x)
- pnpm (use corepackaged version)

## Setup

- Clone this project
- Run `pnpm install` to install dependencies
- Run `pnpm dev` to start the development server
